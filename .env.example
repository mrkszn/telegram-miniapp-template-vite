# Base URL of the backend API the Mini App talks to.
VITE_API_BASE_URL=http://localhost:8000

# Endpoint that exchanges Telegram initData for a JWT.
# Will be called as POST `${VITE_API_BASE_URL}${VITE_AUTH_ENDPOINT}` with body { init_data }.
VITE_AUTH_ENDPOINT=/auth/telegram
